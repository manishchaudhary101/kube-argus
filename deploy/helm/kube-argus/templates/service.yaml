apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-argus.fullname" . }}
  labels:
    {{- include "kube-argus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-argus.selectorLabels" . | nindent 4 }}
