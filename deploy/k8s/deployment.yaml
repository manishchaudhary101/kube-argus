apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-argus
  namespace: kube-argus
  labels:
    app: kube-argus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-argus
  template:
    metadata:
      labels:
        app: kube-argus
    spec:
      serviceAccountName: kube-argus
      containers:
        - name: kube-argus
          image: ghcr.io/manishchaudhary101/kube-argus:latest
          ports:
            - containerPort: 8080
              name: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          envFrom:
            - secretRef:
                name: kube-argus-config
                optional: true
          env:
            - name: PORT
              value: "8080"
            - name: CLUSTER_NAME
              value: "my-cluster"  # Replace with your cluster name
---
apiVersion: v1
kind: Service
metadata:
  name: kube-argus
  namespace: kube-argus
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      name: http
  selector:
    app: kube-argus
